vagrant up            			# create vms
vagrant ssh [name] 				# ssh into vm
vagrant halt          			# shut down vms
vagrant destroy [-f]     		# delete VMs
vagrant global-status --prune	#rm all broken vms
